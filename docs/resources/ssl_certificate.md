---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "zone_ssl_certificate Resource - zone"
subcategory: "Certificates"
description: |-
  A TLS certificate installed on a webhosting service.
  ~> private_key is a write-only argument and needs Terraform 1.11 or newer. The key is sent when the certificate is installed and is never written to state. That is the whole point: state is not an encrypted store, and a private key in it is a private key in every backup and every CI artefact that state passes through. It also matches the API, which accepts the key and returns it as an empty string.
  ~> Changing only the key produces no plan. Terraform cannot detect a change to a value it does not keep. In practice this rarely matters, because a new key comes with a new certificate, which is in state, and an update re-sends both. To force a re-upload on its own, use terraform apply -replace.
---

# zone_ssl_certificate (Resource)

A TLS certificate installed on a webhosting service.

~> **`private_key` is a write-only argument and needs Terraform 1.11 or newer.** The key is sent when the certificate is installed and is never written to state. That is the whole point: state is not an encrypted store, and a private key in it is a private key in every backup and every CI artefact that state passes through. It also matches the API, which accepts the key and returns it as an empty string.

~> **Changing only the key produces no plan.** Terraform cannot detect a change to a value it does not keep. In practice this rarely matters, because a new key comes with a new `certificate`, which *is* in state, and an update re-sends both. To force a re-upload on its own, use `terraform apply -replace`.

## Example Usage

```terraform
# private_key is a write-only argument: it is sent to zone.eu and never written
# to state. That needs Terraform 1.11 or newer.
resource "zone_ssl_certificate" "example" {
  service_name = "virt1.example.com"
  name         = "example.com 2026"

  certificate    = file("${path.module}/certs/example.com.crt")
  ca_certificate = file("${path.module}/certs/example.com.chain.crt")
  private_key    = file("${path.module}/certs/example.com.key")

  hosts = ["example.com", "www.example.com"]
}

output "expires" {
  value = zone_ssl_certificate.example.expires
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

> **NOTE**: [Write-only arguments](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments) are supported in Terraform 1.11 and later.

- `certificate` (String) The certificate itself, PEM encoded. Pass the file's contents, for example `file("cert.pem")`.
- `name` (String) A name for the certificate, shown in the ZoneID panel.
- `private_key` (String, Sensitive, [Write-only](https://developer.hashicorp.com/terraform/language/resources/ephemeral#write-only-arguments)) The certificate's private key, PEM encoded. Write-only: it is sent to zone.eu and never stored in state.
- `service_name` (String) The webhosting service this belongs to. This is a virtual server name, which often looks like a domain but is a different namespace from a DNS zone — use the `zone_vservers` data source to list the valid values. Changing it moves the resource to another service, which means replacing it.

### Optional

- `ca_certificate` (String) The issuer chain, PEM encoded. Most certificates need one, or clients that do not already hold the intermediate will reject the connection.
- `hosts` (Set of String) Hostnames on the service that should use this certificate.

### Read-Only

- `common_name` (String) The certificate's common name, as zone.eu parsed it.
- `connected` (Boolean) Whether the certificate is in use by an SSL service.
- `created` (String) When the certificate was installed.
- `expires` (String) When the certificate expires.
- `id` (String) Identifier assigned by zone.eu. Documented as a number and returned as a string, so it is a string here.
- `letsencrypt` (Boolean) Whether this is one of zone.eu's automatically renewed Let's Encrypt certificates. Those are managed by zone.eu, not by this provider.
- `resource_url` (String) API URL of this certificate.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Certificates are imported as "<service name>/<certificate id>". The private
# key is not imported, because no state ever holds one; the next apply sends
# whatever the configuration supplies.
terraform import zone_ssl_certificate.example "virt1.example.com/163957"
```
